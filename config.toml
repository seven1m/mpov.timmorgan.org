baseURL = 'https://mpov.timmorgan.org/'
locale = 'en-us'
title = 'My Point of View'
theme = 'mini'
[params]
  avatarLink = '/'
  bio = 'technology, programming, and rants (not necessarily in that order)'
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
